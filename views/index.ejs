<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags always come first -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta http-equiv="x-ua-compatible" content="ie=edge" />
    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
    />

    <title>Login</title>
  </head>

  <body>
    <form name="loginmobile" action="/" method="POST">
      <header class="jumbotron bg-light text-black">
        <div class="container">
          <div class="row row-header">
            <div class="col-12 col-sm-6">
              <div class="col-xs-2">
                <img
                  class="img-responsive"
                  src="https://cache-backend-mcd.mcdonaldscupones.com/media/image/product$kzXCTbnv/200/200/original?country=br"
                  alt=""
                  style="width: 160px; height: 120px"
                />
              </div>

              <% if (user) { %>
              <h2 class="text-black">Hello, <%= user.displayName %>.</h2>
              <a href="/logout">Logout</a>
              <% } else {%>
              <h2 class="text-black">Olá! Por favor identifique-se.</h2>
              <% } %>
            </div>
          </div>
        </div>
      </header>

      <div class="container">
        <% if (!user) { %>

        <div class="row row-content align-items-center">
          <div class="form-group row">
            <div class="offset-2 col-10">
              <a
                href="/auth/google"
                class="btn btn-secondary"
                style="width: 150px"
                >Continuar com Google</a
              >
            </div>
          </div>
        </div>

        <div class="row row-content align-items-center">
          <div class="form-group row">
            <div class="offset-2 col-10">
              <a
                href="/auth/facebook"
                class="btn btn-secondary"
                style="width: 150px"
                >Continuar com Facebook</a
              >
            </div>
          </div>
        </div>
        <% } %>
      </div>

      <footer class="footer">
        <div class="container">
          <div class="row">
            <div class="col-12 col-sm-4 align-self-center">
              <div class="text-center"></div>
            </div>
          </div>
        </div>
      </footer>
    </form>
    <!-- jQuery first, then Popper.js, then Bootstrap JS. -->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
  </body>
</html>
